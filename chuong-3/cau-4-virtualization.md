# Câu 4: Process-level VM vs System-level VM

> **Chương:** 3 - Tiến trình và Ảo hóa  
> **Độ khó:** ⭐⭐⭐⭐ (Khó)  
> **Thời gian đọc:** ~20 phút

---

## 📋 Mục lục

- [Đề bài](#đề-bài)
- [Phần 1: Process-Level VM (JVM)](#phần-1-process-level-vm-jvm)
- [Phần 2: System-Level VM (Hypervisor)](#phần-2-system-level-vm-hypervisor)
- [Phần 3: Performance Comparison](#phần-3-performance-comparison)
- [Phần 4: Use Cases](#phần-4-use-cases)
- [Tóm tắt](#tóm-tắt)

---

## 📋 Đề bài

So sánh hai loại virtualization:

**Process-Level VM (JVM - Java Virtual Machine)**
- Virtualize instruction set cho một programming language
- Performance: 80-95% native speed
- Portability: "Write once, run anywhere"

**System-Level VM (Hypervisor)**
- Virtualize toàn bộ hardware
- Performance: 95-98% native speed
- Isolation: Strong isolation giữa VMs

**Yêu cầu:**

1. Giải thích cách mỗi loại VM hoạt động:
   - Process-level: JIT compilation, bytecode
   - System-level: Hardware virtualization, paravirtualization

2. So sánh về:
   - Performance overhead
   - Resource isolation
   - Portability
   - Use cases

3. Phân tích trade-offs cho cloud computing applications

---

## 💡 Bài giải

### Phần 1: Process-Level VM (JVM)

#### A. Architecture
```
╔═══════════════════════════════════════════════╗
║         JAVA VIRTUAL MACHINE (JVM)            ║
╠═══════════════════════════════════════════════╣
║                                               ║
║  ┌─────────────────────────────────────────┐ ║
║  │        JAVA APPLICATION                  │ ║
║  │  ┌──────────────────────────────────┐   │ ║
║  │  │  MyApp.class (bytecode)          │   │ ║
║  │  │                                  │   │ ║
║  │  │  public class MyApp {            │   │ ║
║  │  │    public static void main() {   │   │ ║
║  │  │      System.out.println("Hi");  │   │ ║
║  │  │    }                             │   │ ║
║  │  │  }                               │   │ ║
║  │  └──────────────────────────────────┘   │ ║
║  └──────────────────┬──────────────────────┘ ║
║                     │ .class file              ║
║  ┌──────────────────▼──────────────────────┐ ║
║  │           CLASS LOADER                   │ ║
║  │  - Loads .class files                   │ ║
║  │  - Verifies bytecode                    │ ║
║  └──────────────────┬──────────────────────┘ ║
║                     │                          ║
║  ┌──────────────────▼──────────────────────┐ ║
║  │         EXECUTION ENGINE                 │ ║
║  │                                         │ ║
║  │  ┌─────────────────────────────────┐   │ ║
║  │  │  Interpreter                    │   │ ║
║  │  │  - Executes bytecode line-by-line │ ║
║  │  │  - Slow (10× slower than native)│   │ ║
║  │  └─────────────────────────────────┘   │ ║
║  │                                         │ ║
║  │  ┌─────────────────────────────────┐   │ ║
║  │  │  JIT Compiler ✅                │   │ ║
║  │  │  - Compiles hot code to native   │   │ ║
║  │  │  - Optimizes based on profiling  │   │ ║
║  │  │  - 80-95% native performance ✅  │   │ ║
║  │  └─────────────────────────────────┘   │ ║
║  │                                         │ ║
║  │  ┌─────────────────────────────────┐   │ ║
║  │  │  Garbage Collector              │   │ ║
║  │  │  - Automatic memory management   │   │ ║
║  │  └─────────────────────────────────┘   │ ║
║  └─────────────────┬───────────────────────┘ ║
║                    │ Native instructions      ║
║  ═════════════════════════════════════════   ║
║                    │                          ║
║  ┌─────────────────▼───────────────────────┐ ║
║  │         OPERATING SYSTEM                 │ ║
║  │       (Windows / Linux / macOS)          │ ║
║  └─────────────────┬───────────────────────┘ ║
║                    │                          ║
║  ┌─────────────────▼───────────────────────┐ ║
║  │            HARDWARE (x86/ARM)            │ ║
║  └──────────────────────────────────────────┘ ║
║                                               ║
╚═══════════════════════════════════════════════╝

Key characteristics:
✅ Platform-independent bytecode
✅ JIT compilation for performance
✅ Automatic memory management
✅ Sandboxed execution (security)
⚠️ 5-20% performance overhead
⚠️ Startup time (JIT warm-up)
```

#### B. How JVM Works

**Step 1: Compilation to Bytecode**
```java
// Source code: HelloWorld.java
public class HelloWorld {
    public static void main(String[] args) {
        int x = 5;
        int y = 10;
        int sum = x + y;
        System.out.println(sum);
    }
}

// Compile to bytecode
$ javac HelloWorld.java
// Generates: HelloWorld.class
```

**Bytecode (disassembled):**
```
public static void main(java.lang.String[]);
  Code:
   0: iconst_5          // Push 5 onto stack
   1: istore_1          // Store in local variable 1 (x)
   2: bipush 10         // Push 10
   4: istore_2          // Store in local variable 2 (y)
   5: iload_1           // Load x
   6: iload_2           // Load y
   7: iadd              // Add (x + y)
   8: istore_3          // Store result (sum)
   9: getstatic #2      // Get System.out
  12: iload_3           // Load sum
  13: invokevirtual #3  // Call println(sum)
  16: return
```

**Step 2: JIT Compilation**
```
First execution (Interpreted):
════════════════════════════════════════════
iconst_5    → mov eax, 5
istore_1    → mov [ebp-4], eax
bipush 10   → mov eax, 10
...

Execution time: ~100µs (slow) ⚠️

════════════════════════════════════════════

After 1000 executions (JIT kicks in):
════════════════════════════════════════════
JIT compiler detects "hot" method
Compiles entire method to native code:

; Optimized native x86 code
mov eax, 5           ; x = 5
mov ecx, 10          ; y = 10
add eax, ecx         ; sum = x + y
push eax
call printf          ; System.out.println
ret

Execution time: ~1µs (100× faster!) ✅

════════════════════════════════════════════

Optimizations applied:
✅ Inlining
✅ Dead code elimination
✅ Loop unrolling
✅ Escape analysis
```

#### C. Performance Analysis
```
╔═══════════════════════════════════════════════╗
║       JVM PERFORMANCE CHARACTERISTICS         ║
╠═══════════════════════════════════════════════╣
║                                               ║
║  Phase           │ Performance  │ Notes       ║
║ ═════════════════╪══════════════╪════════════║
║                                               ║
║  Startup         │ Slow ⚠️      │ 1-2 seconds ║
║  (cold)          │ (interpreted)│ JVM init    ║
║                                               ║
║  Warm-up         │ Medium ⚠️    │ 10-30 sec   ║
║  (JIT compiling) │ 50-70%       │ Profiling   ║
║                                               ║
║  Steady-state    │ Fast ✅      │ After 1 min ║
║  (JIT done)      │ 80-95%       │ Optimized   ║
║                                               ║
║  Peak            │ Very fast ✅✅│ Can exceed  ║
║  (with C2 JIT)   │ 95-105%!     │ native      ║
║                                               ║
╚═══════════════════════════════════════════════╝

Benchmark: Compute-intensive task
─────────────────────────────────────────────
Native C:         1.00s (baseline)
JVM (cold):       10.00s (10× slower)
JVM (warm):       1.10s (10% slower)
JVM (peak):       0.95s (5% faster!) ✅

How JVM can be faster than C:
1. Runtime profiling (knows actual data)
2. Aggressive optimizations (can speculate)
3. Modern JIT techniques (LLVM-level)
```

---

### Phần 2: System-Level VM (Hypervisor)

#### A. Architecture
```
╔═══════════════════════════════════════════════╗
║          SYSTEM-LEVEL VIRTUALIZATION          ║
║              (Type 1 Hypervisor)              ║
╠═══════════════════════════════════════════════╣
║                                               ║
║  ┌──────────────┐  ┌──────────────┐          ║
║  │   VM 1       │  │   VM 2       │          ║
║  │              │  │              │          ║
║  │  ┌────────┐  │  │  ┌────────┐  │          ║
║  │  │  App   │  │  │  │  App   │  │          ║
║  │  └────────┘  │  │  └────────┘  │          ║
║  │  ┌────────┐  │  │  ┌────────┐  │          ║
║  │  │ Libs   │  │  │  │ Libs   │  │          ║
║  │  └────────┘  │  │  └────────┘  │          ║
║  │  ┌────────┐  │  │  ┌────────┐  │          ║
║  │  │Guest OS│  │  │  │Guest OS│  │          ║
║  │  │(Linux) │  │  │  │(Windows)│  │          ║
║  │  └────────┘  │  │  └────────┘  │          ║
║  └──────┬───────┘  └──────┬───────┘          ║
║         │                 │                   ║
║  ═══════╪═════════════════╪══════════════    ║
║         │ Virtual Hardware│                   ║
║  ┌──────▼─────────────────▼──────────────┐   ║
║  │         HYPERVISOR (VMM)              │   ║
║  │                                       │   ║
║  │  ┌─────────────────────────────────┐ │   ║
║  │  │  CPU Virtualization             │ │   ║
║  │  │  - Binary translation           │ │   ║
║  │  │  - Hardware-assisted (VT-x)     │ │   ║
║  │  └─────────────────────────────────┘ │   ║
║  │                                       │   ║
║  │  ┌─────────────────────────────────┐ │   ║
║  │  │  Memory Virtualization          │ │   ║
║  │  │  - Shadow page tables (legacy)  │ │   ║
║  │  │  - EPT/NPT (hardware-assisted)  │ │   ║
║  │  └─────────────────────────────────┘ │   ║
║  │                                       │   ║
║  │  ┌─────────────────────────────────┐ │   ║
║  │  │  I/O Virtualization             │ │   ║
║  │  │  - Emulation (slow)             │ │   ║
║  │  │  - Paravirtualization (fast)    │ │   ║
║  │  │  - SR-IOV (hardware passthrough)│ │   ║
║  │  └─────────────────────────────────┘ │   ║
║  └───────────────┬───────────────────────┘   ║
║                  │                            ║
║  ════════════════╪════════════════════════   ║
║                  │                            ║
║  ┌───────────────▼──────────────────────┐    ║
║  │         PHYSICAL HARDWARE            │    ║
║  │  - CPU (Intel VT-x / AMD-V)          │    ║
║  │  - Memory (EPT / NPT)                │    ║
║  │  - NICs, Disks, etc.                 │    ║
║  └──────────────────────────────────────┘    ║
║                                               ║
╚═══════════════════════════════════════════════╝

Key characteristics:
✅ Full hardware virtualization
✅ Strong isolation (separate OS)
✅ 95-98% native performance
✅ Live migration possible
⚠️ Higher overhead than process-level
⚠️ Requires hardware support (VT-x/AMD-V)
```

#### B. How Hypervisor Works

**CPU Virtualization:**
```
Problem: Guest OS tries to execute privileged instruction
════════════════════════════════════════════════════════

Guest VM:
┌────────────────────────────────────────┐
│  Guest OS tries:                       │
│  mov cr3, eax   ; Change page table    │
│  (Privileged instruction!)             │
└────────────────────────────────────────┘

Solution 1: Binary Translation (Software)
════════════════════════════════════════════════════════
Hypervisor intercepts and translates:
mov cr3, eax  →  call vmm_set_cr3(eax)

Performance: 80-85% native ⚠️

Solution 2: Hardware-Assisted (Intel VT-x)
════════════════════════════════════════════════════════
CPU supports:
- VMX root mode (hypervisor)
- VMX non-root mode (guest)

Guest executes privileged instruction:
  → CPU traps to hypervisor (VM exit)
  → Hypervisor handles it
  → CPU returns to guest (VM entry)

VM exit/entry: ~500-1000 cycles
Performance: 95-98% native ✅
```

**Memory Virtualization:**
```
Challenge: Translate Guest Virtual → Guest Physical → Host Physical
═════════════════════════════════════════════════════════════════

Guest VM believes:
Virtual Address 0x1000 → Physical Address 0x5000

But on host:
Guest Physical 0x5000 → Host Physical 0x3F000

Solution 1: Shadow Page Tables (Software)
═════════════════════════════════════════════════════════════════
Hypervisor maintains shadow page table:
Guest VA 0x1000 → Host PA 0x3F000 (direct mapping)

Update on every guest page table change
Performance: 70-80% native ❌

Solution 2: Extended Page Tables - EPT (Hardware)
═════════════════════════════════════════════════════════════════
CPU hardware does two-level translation:

1. Guest VA → Guest PA (guest page table)
2. Guest PA → Host PA (EPT, maintained by hypervisor)

CPU caches translations in TLB
Performance: 95-98% native ✅
```

**I/O Virtualization:**
```
╔═══════════════════════════════════════════════╗
║          I/O VIRTUALIZATION METHODS           ║
╠═══════════════════════════════════════════════╣
║                                               ║
║  1. Full Emulation (Software) ⚠️             ║
║     ┌──────────────────────────────────────┐ ║
║     │ Guest driver writes to emulated NIC  │ ║
║     │  → Trap to hypervisor                │ ║
║     │  → Hypervisor emulates in software   │ ║
║     │  → Slow (10-20% of native) ❌        ║
║     └──────────────────────────────────────┘ ║
║                                               ║
║  2. Paravirtualization (Virtio) ✅           ║
║     ┌──────────────────────────────────────┐ ║
║     │ Guest uses special virtio driver     │ ║
║     │  → Knows it's virtualized            │ ║
║     │  → Direct communication with hypervsr│ ║
║     │  → Fast (70-80% of native) ✅        ║
║     └──────────────────────────────────────┘ ║
║                                               ║
║  3. SR-IOV (Hardware Passthrough) ✅✅        ║
║     ┌──────────────────────────────────────┐ ║
║     │ Physical NIC exposed to VM directly  │ ║
║     │  → No hypervisor intervention        │ ║
║     │  → Near-native (95-99%) ✅✅          ║
║     │  → But: No live migration ⚠️         │ ║
║     └──────────────────────────────────────┘ ║
║                                               ║
╚═══════════════════════════════════════════════╝
```

#### C. Performance Analysis
```
╔═══════════════════════════════════════════════╗
║      HYPERVISOR PERFORMANCE BREAKDOWN         ║
╠═══════════════════════════════════════════════╣
║                                               ║
║  Component      │ Method         │ Overhead  ║
║ ════════════════╪════════════════╪══════════ ║
║                                               ║
║  CPU            │ Hardware (VT-x)│   2-5%    ║
║                 │ Binary transl. │  15-20%   ║
║                                               ║
║  Memory         │ EPT/NPT        │   2-3%    ║
║                 │ Shadow PT      │  20-30%   ║
║                                               ║
║  Disk I/O       │ Paravirt       │   5-10%   ║
║                 │ Emulation      │  50-80%   ║
║                                               ║
║  Network I/O    │ SR-IOV         │   1-5%    ║
║                 │ Paravirt       │  20-30%   ║
║                 │ Emulation      │  80-90%   ║
║                                               ║
║  Overall        │ Modern (HW)    │   2-5% ✅ ║
║  (best config)  │                │           ║
║                                               ║
╚═══════════════════════════════════════════════╝

Real-world benchmark:
─────────────────────────────────────────────
Workload: Web server (Nginx)

Native (bare metal):     50,000 req/s
VM (modern hypervisor):  48,500 req/s
Overhead:                3% ✅

Workload: Database (PostgreSQL)

Native:                  10,000 TPS
VM (with SR-IOV):        9,700 TPS
Overhead:                3% ✅
```

---

### Phần 3: Performance Comparison
```
╔═══════════════════════════════════════════════════════╗
║       PROCESS-LEVEL VM vs SYSTEM-LEVEL VM             ║
╠═══════════════════════════════════════════════════════╣
║                                                       ║
║  Metric              │ JVM          │ Hypervisor     ║
║                      │ (Process-VM) │ (System-VM)    ║
║ ═════════════════════╪══════════════╪═══════════════ ║
║                                                       ║
║  Performance         │ 80-95%       │ 95-98% ✅      ║
║  (steady-state)      │              │                ║
║                                                       ║
║  Startup time        │ 1-2 sec ⚠️   │ 30-60 sec ⚠️   ║
║                      │              │ (boot OS)      ║
║                                                       ║
║  Memory overhead     │ 50-200 MB    │ 512 MB-2 GB ⚠️ ║
║                      │ (per JVM)    │ (per VM)       ║
║                                                       ║
║  Isolation           │ Weak ⚠️      │ Strong ✅      ║
║                      │ (process)    │ (hardware)     ║
║                                                       ║
║  Portability         │ High ✅      │ Low ⚠️         ║
║                      │ (bytecode)   │ (VM image)     ║
║                                                       ║
║  Resource sharing    │ Easy ✅      │ Hard ⚠️        ║
║                      │ (same OS)    │ (separate OS)  ║
║                                                       ║
║  Security            │ Sandboxed    │ Isolated ✅✅   ║
║                      │ (JVM)        │ (hardware)     ║
║                                                       ║
║  Density             │ High ✅      │ Medium ⚠️      ║
║  (instances/server)  │ 100s         │ 10s            ║
║                                                       ║
╚═══════════════════════════════════════════════════════╝
```

---

### Phần 4: Use Cases
```
╔═══════════════════════════════════════════════╗
║              USE CASE ANALYSIS                ║
╠═══════════════════════════════════════════════╣
║                                               ║
║  Scenario              │ Best Choice         ║
║ ═══════════════════════╪════════════════════ ║
║                                               ║
║  Microservices         │ Process-VM (JVM) ✅ ║
║  (same language)       │ Low overhead        ║
║                                               ║
║  Multi-tenant SaaS     │ System-VM ✅        ║
║  (strong isolation)    │ Security critical   ║
║                                               ║
║  Development           │ System-VM ✅        ║
║  environments          │ Match production    ║
║                                               ║
║  Serverless (FaaS)     │ Process-VM ✅       ║
║  (fast startup)        │ Sub-second cold     ║
║                                               ║
║  Legacy apps           │ System-VM ✅        ║
║  (different OS)        │ Full compatibility  ║
║                                               ║
║  Cloud infrastructure  │ System-VM ✅        ║
║  (IaaS)                │ Customer control    ║
║                                               ║
╚═══════════════════════════════════════════════╝
```

---

## 📊 Tóm tắt

### Key Points

- ✅ **Process-level VM (JVM)**: 80-95% perf, portable, fast startup
- ✅ **System-level VM**: 95-98% perf, strong isolation, full OS
- ✅ **JVM wins**: Portability, density, startup time
- ✅ **Hypervisor wins**: Performance, isolation, compatibility
- ✅ **Cloud trend**: Containers > VMs > bare metal

### Trade-offs

| Factor | Process-VM (JVM) | System-VM | Winner |
|--------|------------------|-----------|--------|
| Raw performance | 80-95% | 95-98% | System-VM |
| Startup time | 1-2s | 30-60s | Process-VM ✅ |
| Memory efficiency | 50-200MB | 500MB-2GB | Process-VM ✅ |
| Isolation | Weak | Strong | System-VM ✅ |
| Portability | High | Low | Process-VM ✅ |
| OS flexibility | None | Full | System-VM ✅ |

---

## 🔗 Tài liệu tham khảo

### JVM
- **Inside the Java Virtual Machine** - Bill Venners
- OpenJDK HotSpot: https://openjdk.org/groups/hotspot/

### Hypervisors
- **Virtual Machines** - Smith & Nair
- KVM: https://www.linux-kvm.org/
- Xen: https://xenproject.org/

---

## 🧭 Navigation

**[⬅️ Câu 3: Server Architecture](./cau-3-server-architecture.md)** | **[📚 Quay lại Chương 3](./README.md)** | **[➡️ Câu 5: Hypervisor Comparison](./cau-5-hypervisor-comparison.md)**

---

*Cập nhật lần cuối: 11/12/2025*